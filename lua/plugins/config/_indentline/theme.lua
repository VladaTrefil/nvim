local colors = require('theme.colors')

local config = {
	IndentBlanklineChar = { fg = colors.dark3, bg = colors.dark0 },
	IndentBlanklineContextChar = { fg = colors.faded_blue, bg = colors.dark0 },
	IblScope = { fg = colors.dark4, bg = colors.none },
}

return config
