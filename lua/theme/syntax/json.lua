local colors = require('theme.colors')

local config = {
  jsonKeyword = { link = 'Green' },
  jsonQuote = { link = 'Green' },
  jsonBraces = { link = 'Fg1' },
  jsonString = { link = 'Fg1' },
}

return config
